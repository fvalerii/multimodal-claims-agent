[pytest]
# The evaluation suite lives in main.py (the documented evaluation entry point),
# not a conventional test_*.py file, so widen collection to include it.
python_files = main.py test_*.py
python_functions = test_*
addopts = -ra
